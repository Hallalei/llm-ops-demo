# Database
# If not using docker, set DATABASE_URL to hosted postgres url and ignore the other database variables.
DATABASE_URL="postgresql://tablecn:securepassword@localhost:5432/tablecn"

DATABASE_HOST="localhost"
DATABASE_USER="tablecn"
DATABASE_PASSWORD="securepassword"
DATABASE_NAME="tablecn"
DATABASE_PORT="5432"

# Auth.js
AUTH_SECRET="your-auth-secret"
AUTH_URL="http://localhost:12138"

# LLM API (V-API 镜像 - 兼容 OpenAI 接口)
LLM_API_BASE_URL="https://api.v-api.ai/v1"
VAPI_API_KEY="your-v-api-key"
VAPI_BASE_URL="https://api.v-api.ai/v1"

# Internal API (触发批处理用)
TRANSLATE_SECRET="your-internal-bearer-secret"

# Auto Translation (定时任务)
ENABLE_AUTO_TRANSLATE="false"           # 设为 true 启用自动翻译定时任务
TRANSLATE_CRON_SCHEDULE="*/5 * * * *"   # 执行间隔 (默认每5分钟)
TRANSLATE_BATCH_SIZE="20"               # 每次翻译条数 (默认20条)
TRANSLATE_CONCURRENCY="50"
ENABLE_AUTO_CLASSIFY="false"
CLASSIFY_CRON_SCHEDULE="*/5 * * * *"
CLASSIFY_BATCH_SIZE="200"
CLASSIFY_CONCURRENCY="100"
ENABLE_AUTO_LANGUAGE_DETECT="false"
LANGUAGE_DETECT_CRON_SCHEDULE="*/5 * * * *"
LANGUAGE_DETECT_BATCH_SIZE="200"
LANGUAGE_DETECT_CONCURRENCY="100"

# Public API (仪表盘数据对外接口)
PUBLIC_API_KEY="your-public-api-key"    # 用于外部系统调用仪表盘 API
